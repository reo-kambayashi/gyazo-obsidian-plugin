{
  "name": "gyazo-obsidian-plugin",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian --target=es2020",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.6.2",
    "tslib": "^2.7.0",
    "typescript": "^5.6.3"
  }
}
